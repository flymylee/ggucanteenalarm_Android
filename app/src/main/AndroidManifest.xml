<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cf.sahayana.canteenalarm.ggu">

    <!-- 부팅을 확인 할수 있는 퍼미션 등록 -->
    <!--<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />-->

    <!-- SD카드 기록 허용하는 퍼미션 -->
    <!--<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />-->
    <!-- SD카드 읽기 허용하는 퍼미션, 킷캣이후로는 필요없음 -->
    <!--<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />-->

    <!-- CPU 상태 유지 및 화면 꺼짐 제어를 위한 퍼미션 --> <!--screen on/off-->
    <!--<uses-permission android:name="android.permission.WAKE_LOCK" />-->

    <!-- 인터넷에 접속하기 위한 퍼미션 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--wifi status-->
    <!--<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />-->
    <!--<uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />-->

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--uuid-->
    <!--<uses-permission android:name="android.permission.READ_PHONE_STATE" />-->


    <!--add window view-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name="cf.sahayana.canteenalarm.ggu.CanteenAlarmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="cf.sahayana.canteenalarm.ggu.view.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>